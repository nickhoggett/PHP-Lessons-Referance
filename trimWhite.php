<?php

    // trim() function cuts the white space in a string eg spacebar. 

    $space = rtrim("   username      ");

    // strlen() function counts the amount of charecters in a string. 

    $letCount = strlen($space);
    print $letCount;

    //Also ltrim() function removes any white space from the left handside of the string.
    // rtrim() removes any white spce from the right handside of the string. 
    
?>
